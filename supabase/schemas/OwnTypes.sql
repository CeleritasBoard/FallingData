DO $$
BEGIN
CREATE TYPE PacketType AS ENUM (
    'WELCOME',
    'FLASH_DUMP',
    'HEADER',
    'SPECTRUM',
    'SELFTEST',
    'DEFAULT_STATUS_REPORT',
    'FORCED_STATUS_REPORT',
    'ERROR',
    'GEIGER_COUNT'
);

-- Enum az eszköz típusának (Device)
CREATE TYPE Device AS ENUM (
    'BME_HUNITY',
    'ONIONSAT_TEST',
    'SLOTH'
);

CREATE TYPE CommandType AS ENUM ('SET_DURATION', 'SET_SCALE', 'REQUEST_MEASUREMENT',
    'REQUEST_SELFTEST', 'FORCE_STATUS_REPORT', 'RESET', 'RESTART', 'SAVE', 'STOP_MEASUREMENT');

CREATE TYPE CommandState AS ENUM('CREATED', 'SCHEDULED', 'UPLOADED', 'DELETED');

END$$;